---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ArticleCard from '../../components/ArticleCard.astro';
import { getCollection } from 'astro:content';
import { PROJECT_CATEGORIES, DIFFICULTY_LEVELS } from '../../consts';

// 获取所有项目
const allProjects = await getCollection('projects', ({ data }) => !data.draft);

// 排序：最新的在前
const sortedProjects = allProjects.sort((a, b) => 
  b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);

// 获取所有标签
const allTags = [...new Set(allProjects.flatMap(project => project.data.tags))];
---

<BaseLayout 
  title="项目拆解 - 老刘的生财之道" 
  description="老刘拆解的各种互联网赚钱项目，包含项目分析、操作流程、收入预估等"
>
  <section class="page-header">
    <div class="container">
      <h1>🔍 项目拆解</h1>
      <p>手把手教你拆解各种赚钱项目</p>
    </div>
  </section>
  
  <section class="page-content">
    <div class="container">
      <div class="content-layout">
        <!-- Sidebar -->
        <aside class="sidebar">
          <!-- Categories -->
          <div class="sidebar-section">
            <h3>📂 项目分类</h3>
            <ul class="category-list">
              <li>
                <a href="/projects" class="category-link active">
                  全部项目
                  <span class="count">{sortedProjects.length}</span>
                </a>
              </li>
              {PROJECT_CATEGORIES.map(cat => {
                const count = allProjects.filter(p => p.data.category === cat.slug).length;
                return (
                  <li>
                    <a href={`/projects?category=${cat.slug}`} class="category-link">
                      {cat.name}
                      <span class="count">{count}</span>
                    </a>
                  </li>
                );
              })}
            </ul>
          </div>
          
          <!-- Difficulty Filter -->
          <div class="sidebar-section">
            <h3>📊 难度等级</h3>
            <ul class="category-list">
              {DIFFICULTY_LEVELS.map(level => {
                const count = allProjects.filter(p => p.data.difficulty === level.slug).length;
                return (
                  <li>
                    <a href={`/projects?difficulty=${level.slug}`} class="category-link">
                      <span class="difficulty-badge" style={`--color: ${level.color}`}>
                        {level.name}
                      </span>
                      <span class="count">{count}</span>
                    </a>
                  </li>
                );
              })}
            </ul>
          </div>
          
          <!-- Tags -->
          {allTags.length > 0 && (
            <div class="sidebar-section">
              <h3>🏷️ 热门标签</h3>
              <div class="tag-cloud">
                {allTags.slice(0, 15).map(tag => (
                  <a href={`/projects?tag=${tag}`} class="tag">{tag}</a>
                ))}
              </div>
            </div>
          )}
        </aside>
        
        <!-- Main Content -->
        <main class="main-content">
          {sortedProjects.length === 0 ? (
            <div class="empty-state">
              <p>📭 暂无项目</p>
              <p class="text-muted">老刘正在拆解项目中...</p>
            </div>
          ) : (
            <div class="projects-grid">
              {sortedProjects.map((project) => (
                <ArticleCard
                  title={project.data.title}
                  description={project.data.description}
                  pubDate={project.data.pubDate}
                  heroImage={project.data.heroImage}
                  category={project.data.category}
                  tags={project.data.tags}
                  href={`/projects/${project.id}`}
                  type="project"
                  difficulty={project.data.difficulty}
                  estimatedIncome={project.data.estimatedIncome}
                />
              ))}
            </div>
          )}
        </main>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .page-header {
    background: linear-gradient(135deg, var(--accent-dark) 0%, var(--accent) 100%);
    color: var(--text-primary);
    padding: 3rem 0;
    text-align: center;
  }
  
  .page-header h1 {
    margin: 0 0 0.5rem 0;
    color: var(--primary-dark);
  }
  
  .page-header p {
    margin: 0;
    color: var(--primary-dark);
    opacity: 0.9;
  }
  
  .page-content {
    padding: 3rem 0;
  }
  
  .content-layout {
    display: grid;
    grid-template-columns: 280px 1fr;
    gap: 3rem;
  }
  
  /* Sidebar */
  .sidebar {
    position: sticky;
    top: calc(var(--header-height) + 2rem);
    align-self: start;
  }
  
  .sidebar-section {
    background: var(--bg-secondary);
    border-radius: var(--radius-lg);
    padding: 1.5rem;
    margin-bottom: 1.5rem;
  }
  
  .sidebar-section h3 {
    font-size: 1rem;
    margin: 0 0 1rem 0;
    color: var(--text-primary);
  }
  
  .category-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  
  .category-list li {
    margin-bottom: 0.25rem;
  }
  
  .category-link {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.5rem 0.75rem;
    color: var(--text-secondary);
    text-decoration: none;
    border-radius: var(--radius);
    transition: all var(--transition);
  }
  
  .category-link:hover,
  .category-link.active {
    background: var(--bg-tertiary);
    color: var(--primary);
  }
  
  .category-link .count {
    font-size: 0.75rem;
    background: var(--bg-tertiary);
    padding: 0.125rem 0.5rem;
    border-radius: var(--radius-full);
    color: var(--text-muted);
  }
  
  .category-link.active .count,
  .category-link:hover .count {
    background: var(--primary);
    color: var(--text-inverse);
  }
  
  .difficulty-badge {
    display: inline-block;
    padding: 0.125rem 0.5rem;
    background: var(--color);
    color: white;
    border-radius: var(--radius-sm);
    font-size: 0.75rem;
    font-weight: 500;
  }
  
  .tag-cloud {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }
  
  /* Main Content */
  .projects-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
  }
  
  .empty-state {
    text-align: center;
    padding: 4rem 2rem;
    background: var(--bg-secondary);
    border-radius: var(--radius-lg);
  }
  
  .empty-state p:first-child {
    font-size: 2rem;
    margin-bottom: 0.5rem;
  }
  
  /* Responsive */
  @media (max-width: 1024px) {
    .content-layout {
      grid-template-columns: 1fr;
    }
    
    .sidebar {
      position: static;
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1.5rem;
    }
    
    .sidebar-section {
      margin-bottom: 0;
    }
  }
  
  @media (max-width: 768px) {
    .page-header {
      padding: 2rem 0;
    }
    
    .sidebar {
      grid-template-columns: 1fr;
    }
    
    .projects-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
